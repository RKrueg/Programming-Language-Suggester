function languages(event) {
  event.preventDefault();

  let ruby = document.querySelector();
  let cSharp = document.querySelector();
  let swift = document.querySelector();


window.addEventListener("load", function () {
  const form = this.document.getElementById("language");
  form.addEventListener("submit", languages)
})
}